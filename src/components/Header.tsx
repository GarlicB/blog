function Header() {
  return (
    <div
      className="header"
      style={{
        display: "flex",
        alignItems: "center",
        height: "40px",
        padding: "0 1rem",
        textAlign: "left",
      }}
    >
      GarlicB.log
    </div>
  );
}

export default Header;
